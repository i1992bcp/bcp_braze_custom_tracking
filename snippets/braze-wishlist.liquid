(function () {
  if (!window.SwymCallbacks) {
    window.SwymCallbacks = window.SwymCallbacks || [];
  }
  window.SwymCallbacks.push(function () {
    _swat.fetchWrtEventType(function (products) {
        let _wishProducts = products;
        // console.log(products);
        
        BrazeDataLayer.ecommerce.wishlist.push(_wishProducts);
    });
  });
})();

// console.log('Wishlist');
// // Wishlist Product Added to Cart
// $('button.custom-wishlist-add-to-cart')
// .click(function(){
//     // appboy.logCustomEvent("Wishlist Product Added to Cart", {
//     //     product_id: $(this).attr("data-product-id") ,
//     //     price: $(this).attr("data-product-price") 
//     // });
//     console.log('Product Added To Wishlist');
// });
$('.custom-wishlist-add-to-cart')
.click(() => {
    console.log('Added To Cart from Wishlist');
})